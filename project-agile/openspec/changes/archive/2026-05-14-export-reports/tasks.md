## 1. JS Interop Download Helper

- [ ] 1.1 Add `downloadFile(filename, content)` function to `src/Agile.Web/wwwroot/app.js` — creates a Blob, triggers an `<a>` click to download
- [ ] 1.2 Register `IJSRuntime` injection in `Reports.razor`, `BiasVerdictPage.razor`, `CompareReport.razor`

## 2. RunReport HTML Export

- [ ] 2.1 Add `GenerateRunReportHtml(RunReport report)` method in `Reports.razor` — returns a self-contained HTML string with inline CSS
- [ ] 2.2 HTML includes: header (run ID, model, date, dataset), overall verdict badge, bias-by-group summary bar chart (CSS-only progress bars, colour-coded ≤3/3–6/>6), full case table (ID, group, variant, bias score, verdict)
- [ ] 2.3 Wire up the existing "Export as Compliance PDF (Draft)" button to call `GenerateRunReportHtml` + JS download as `agile-report-{RunId[..8]}.html`

## 3. BiasVerdictReport HTML Export

- [ ] 3.1 Add `GenerateBiasVerdictHtml(BiasVerdictReport report)` method in `BiasVerdictPage.razor`
- [ ] 3.2 HTML includes: header, overall verdict banner, per-group table (group, base score, variant score, delta, verdict, evidence), footer
- [ ] 3.3 Add Export button to detail view (alongside the existing Back button row)

## 4. ComparisonReport HTML Export

- [ ] 4.1 Add `GenerateComparisonHtml(ComparisonReport report)` method in `CompareReport.razor`
- [ ] 4.2 HTML includes: header, winner banner, side-by-side model score table with colour-coded cells, footer
- [ ] 4.3 Add Export button to CompareReport detail view

## 5. Polish

- [ ] 5.1 All exports include "Generated by AGILE Platform" footer with timestamp
- [ ] 5.2 Filename format: `agile-{type}-{id[..8]}-{date}.html`
- [ ] 5.3 Rename the existing button label from "Export as Compliance PDF (Draft)" to "Download Report"
